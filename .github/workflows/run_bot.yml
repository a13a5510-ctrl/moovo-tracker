name: Moovo Auto Tracker

on:
  schedule:
    - cron: '*/15 * * * *' # 保留原本的當作備用
  workflow_dispatch:
  repository_dispatch:      # 👈 新增這行：允許接收外部 Webhook 訊號
    types: [moovo-cron-trigger] # 👈 設定一個專屬的通關密語

jobs:
  run-bot:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - name: 1. Checkout
        uses: actions/checkout@v4

      - name: 2. Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 3. 安裝工具與瀏覽器
        run: |
          pip install -r requirements.txt
          python -m playwright install chromium

      - name: 3.5 還原衛星金鑰
        run: |
          echo '${{ secrets.GCP_SERVICE_ACCOUNT_JSON }}' > creds.json

      - name: 4. 啟動情報監測
        env:
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PYTHONIOENCODING: utf-8
        run: python myapp.py

      - name: 5. 提交記憶數據
        shell: bash
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          if [ -n "$(git status --porcelain)" ]; then
            git add last_data.json history_trend.json
            git commit -m "Update tracker data [skip ci]"
            git push
          else
            echo "數據無變動，無需更新記憶。"
          fi
